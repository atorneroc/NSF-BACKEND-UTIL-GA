apiVersion: apps/v1
kind: Deployment
metadata:
  name: facturacion-deployment-util
  labels:
    app: facturacion
    tier: service-util
spec:
  replicas: 1
  strategy:
    rollingUpdate:
        maxSurge: 1
        maxUnavailable: 1
    type: RollingUpdate
  selector:
    matchLabels:
      app: facturacion
      tier: service-util
  template:
    metadata:
      labels:
        app: facturacion
        tier: service-util
    spec:
      containers:
      - name: facturacion-service-util
        image: scharffnsf.azurecr.io/facturacion/prod/service-util:__Build.BuildId__

        ports:
        - containerPort: 5002
        resources:
          requests: # minimum resources required
            cpu: 250m
            memory: 300Mi
          limits: # maximum resources allocated
            cpu: 450m
            memory: 750Mi
        
        volumeMounts:
        - name: secrets
          mountPath: /app/secrets
          readOnly: true
        
      imagePullSecrets:
      - name: acr-secret
        
      volumes:
      - name: secrets
        secret:
          secretName: secret-appsettings